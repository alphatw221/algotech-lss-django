build-job:
    stage: build
    before_script:
        - apt update
        - apt install python3
        - python3 --version  # For debugging
        - pip install poetry
    script: echo "build"

unit-test:
    stage: test
    script: echo "unit-test"

lint-test:
    stage: test
    script: echo "lint-test"


deploy-app:
  stage: deploy
  script: echo "deploment"
